import React from "react";
const CardComponent = async ({ user }) => {
  // const name = user.username;
  // return <div>{name}</div>;

  return (
    <div>
      <span></span>
    </div>
  );
};

export default CardComponent;
